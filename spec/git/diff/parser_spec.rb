require 'spec_helper'

describe Git::Diff::Parser do
  it 'has a version number' do
    expect(Git::Diff::Parser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
